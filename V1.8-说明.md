# ClubOS V1.8 — 修复说明

## V2.1 核心体验升级（2026-08-31）

- 内容方向依据距离、海拔、天数、年龄、名额、时间窗口和已确认服务动态生成，不再随机套固定三种风格。
- 客户页面只使用老板输入或手动确认的事实；规则推断的年龄、难度、服务和保障不会直接发布。
- 一句话中的“行程：”可直接拆成真实时间轴；不自动编造费用不含、领队资质与安全承诺。
- 亲子、城市、高海拔、露营和徒步使用不同阅读顺序与视觉语法；图片区块随素材数量自动变化且不重复用图。
- 上传图片后自动分析焦点、方向和质量并推荐封面，老板手动指定后不再被覆盖。
- 发布前检查关键事实，高风险活动的难度和适合人群必须由老板确认。
- 海报和各平台文案统一围绕同一个本场卖点生成。

## 问题
1. 分享海报人物照片被裁掉（头像丢失）；
2. 详情页文案太少、太单调。

## 根因
- **最关键的**：V1.8 代码改完后一直没重新部署，你测的是上一版，所以两处都没变。
- **一个真实 bug**：`generateCopy` 里分享文案（shareXhs / shareGzh）在生成多段文案的 `applyContentDirection` **之前**用了旧的单段 `a.intro` + 旧 `a.headline`，导致分享文案是旧的、详情页主文案才是对的——不一致。

## 修复
### 海报人物焦点
- `focusValue` 默认焦点 y：58 → 48（避免头部被压）；
- `analyzeImageFocus`：降低「偏下」权重、记录原始分数，若主体明显在上半部则限制焦点上移；
- `drawImageCover`：竖图紧裁时把焦点 y 收在 [0.30, 0.52]，防止头像被切。

### 详情页地点 + 季节文案
- 新增语料库 `PLACE_DB`（都江堰/成都/川西/云南/贵州/西藏/新疆/海南/default），每个地点含四季开场句 + 氛围句；
- `seasonFromActivity` 从日期解析季节；`placeFlavorFor` 返回「季节句 + 地点氛围」；
- 介绍改为多段组合：**季节开场句 + 事实方向句 + 地点氛围句**；
- 所有版式（city/hike/alpine/camp/kids/ski/water/sight）均加入「为什么值得参加」区块；
- 详情页介绍按段落渲染，文案更有层次、更有吸引力。

### 分享文案覆盖顺序
- 把 `applyContentDirection` 提前到生成早期，删除旧单段 `a.intro` 赋值与重复的末尾调用 → 详情页与分享文案统一复用多段文案。

## 校验
- `node --check` 通过；
- Node 冒烟测试 18/18 通过（都江堰 8 月正确解析为夏季季句、intro 生成 3 段、分享文案含多段、8 种版式均含 highlights、详情页渲染 ≥2 段 `<p>` 且含安全 briefing、焦点默认 y=48）。

## 部署
- 已重新部署到云端，可点击测试：
  https://67adb34e2d3c408f8df71bfe26154746.bj3.agentos-app.net

---

# ClubOS V1.8.1 — 画廊修复与版式变化

## 问题
1. 详情页「FIELD NOTES」文案长了之后，把下方图片遮住一半；
2. 每次活动的图片排版都一样，希望根据活动类型变化。

## 根因
`.editorial-gallery` 使用绝对定位 + 固定 `min-height`，`.photo-copy` 随文案变高后溢出，直接盖住下面的 `.photo-c` 或下一个区块。

## 修复
### 自适应高度（不再压图）
- 将 `.editorial-gallery` 从绝对定位改为 **CSS Grid**；
- 删除固定 `min-height`，画廊总高度随文案和图片自然撑开；
- `.photo-copy` 作为独立 grid cell，扩展时只占用自己的格子。

### 三种版式按活动类型自动切换
新增 `galleryLayoutFor(a)`：
- `mosaic`：hike / alpine / ski —— 左大图 + 右文案/小图，户外戏剧感；
- `split`：city / water —— 文案在左、照片竖排在右，干净简洁；
- `cards`：kids / camp / sight —— 大图 + 文案 + 双小图，亲和易读。

### 移动端适配
≤480px 时自动切换为更紧凑的两列/单列布局，保证小屏也不会压图。

## 校验
- `node --check app.js` OK；styles.css 括号 791/791；
- Node 冒烟测试 30/30 通过（含 12 条新增画廊断言：三种 layout 映射正确、HTML 输出含对应 class、无 `position:absolute`）。

## 部署
- 已重新部署，链接同上：
  https://67adb34e2d3c408f8df71bfe26154746.bj3.agentos-app.net

## 备份
- `versions/app.js-详情页画廊自适应与版式变化-v20.js`
- `versions/styles.css-详情页画廊自适应与版式变化-v20.css`

---

---

# ClubOS V1.8.2 — 画廊底层文案 bug + 海报手动换主图

## 问题
1. 详情页「FIELD NOTES」小字显示底层逻辑文案（如「聚焦已确认的路线与完成感」），不是给用户的文案；
2. 分享海报主图固定为活动第一张照片，无法手动替换。

## 根因
- `blockGallery` 直接把 `contentStrategy.reason`（内部策略说明）作为 caption 展示给用户；
- 分享弹窗 `redraw()` 始终读取 `a.photos[0]`，没有提供选择入口，也没有持久化用户偏好。

## 修复
### 画廊 caption 用用户文案
- 新增 `galleryCaption(a)`：优先用 `contentStrategy.headline`，次之用 `intro` 第一句截短，兜底用季节句；
- `blockGallery` 不再使用 `contentStrategy.reason`，避免把底层逻辑暴露给用户。

### 海报支持手动换主图
- 分享弹窗增加「主图」缩略图选择条（多图时显示）；
- 点击缩略图更新 `a.posterPhotoIndex`，立即重绘海报；
- 下次打开弹窗会记住上次选择；分享微信/朋友圈的预览图也跟随该索引。

## 校验
- `node --check app.js` OK；styles.css 括号 796/796；
- Node 冒烟测试 32/32 通过（新增画廊 caption、海报选图相关断言）。

## 部署
- 已重新部署：
  https://67adb34e2d3c408f8df71bfe26154746.bj3.agentos-app.net

---

# ClubOS V1.8.3 — 海报手动换图后空白 + 焦点未跟随

## 问题
1. 手动切换海报主图后，缩略图可点击，但 canvas 海报区域空白，没有显示宣传画面。

## 根因
- `openShare` 里 front 用户分支与后台商家分支共用 `redraw()`，但 `pStyle`/`pSize` 只在后台分支内声明；虽然用户截图是后台版本，但重构后代码在变量作用域和分支覆盖上存在隐患。
- 更重要的是：`drawPoster` 内部固定用 `focusValue((a.photos || [])[0])`，切换主图后焦点仍按第一张图计算，且 `redraw()` 异步加载失败时没有把错误暴露出来，导致 canvas 空白且难以排查。

## 修复
- 把 `pStyle`、`pSize`、`pPhotoIdx`、`photoPicker` 提到 `if/else` 之前，两个分支都具备完整状态与换图能力；
- `drawPoster` 改为 `drawPoster(canvas, a, style, sizeKey, mode, img, focus)`，焦点随当前选中的 `curPhoto` 计算；
- 新增 `drawPosterSafe()` 包装：任何渲染异常都会捕获并在 canvas 上显示红色错误提示，而不是空白；
- `redraw()` 先画「海报生成中...」占位，避免等待期空白；图片/焦点分析失败时兜底绘制无图版本；
- 分享微信/朋友圈的预览图 `imgUrl` 继续跟随 `a.posterPhotoIndex`。

## 校验
- `node --check app.js` OK；
- Node 冒烟测试 32/32 通过；
- 重新部署到同一 CloudStudio 沙盒。

## 部署
- 已重新部署：
  https://67adb34e2d3c408f8df71bfe26154746.bj3.agentos-app.net

## 全部相关备份
- V1.8：`versions/app.js-分享文案覆盖修复-V1.8-v19.js`、`versions/styles.css-修复海报人物裁剪与详情页文案-v18.css`
- V1.8.1：`versions/app.js-详情页画廊自适应与版式变化-v20.js`、`versions/styles.css-详情页画廊自适应与版式变化-v20.css`
- V1.8.2：`versions/app.js-修复画廊底层文案与海报手动选图-v21.js`、`versions/styles.css-修复画廊底层文案与海报手动选图-v21.css`
- V1.8.3：`versions/app.js-修复海报空白与焦点跟随当前图-v23.js`、`versions/styles.css-修复海报空白与焦点跟随当前图-v23.css`

---

# ClubOS V1.9 — 文案智能升级（方向随机化 / 详情页去重 / 数据联动 / 分享润色）

## 问题（用户四类反馈）
1. AI 建议文案方向仅是 3 种固定模板，不结合地点/人文/季节；
2. 详情页图文编排只是基础图片+重复文案，未依要素生成共鸣文案，多次重复；
3. 数据未依要素抓取（5 天徒步误显 8-55 岁/轻松，实际不适合亲子）；
4. 分享文案基本是口水话，无美感无共鸣。

## 修复
- **数据联动**：新增 `recomputeDifficultyAndAge(a)` 并在 `generateCopy` 开头调用；`computeDifficulty`/`recommendAge` 依据天数、强度关键词、亲子标记重算，5 天亲子徒步 → 难度「挑战」+ 年龄「10—14 岁」；
- **方向智能**：`buildContentDirections`/`titleSetFor` 方向池改为 `directionPoolFor` + `scoreDirections`（按地点/类型/季节评分 + 随机权重 `Math.random()*12`），不再是 3 固定模板；
- **详情页去重**：`galleryCaption` 优先 `headline → intro 首句 → seasonLine`；`blockGallery`/`blockAtmosphere` 改写，避免与 intro 重复底层逻辑文案；
- **分享润色**：新增 `buildShareCopy(a)`，按是否亲子/地点/季节语料为 5 平台（微信群/朋友圈/小红书/公众号/口播）生成各异声线文案，无口水话；用 Python 精确 splice 删除 `generateCopy` 内 1179–1229 行坏双写块（先写后覆盖且破坏 highlights/feeInclude），统一改为单写调用。

## 校验
- `node --check app.js` OK；
- Node 冒烟测试 42/42 通过（含 5 日亲子徒步→挑战+10—14 岁、buildShareCopy 五平台非重复、galleryCaption 优先季节句、scoreDirections 随机性增强）；
- 重新部署到同一 CloudStudio 沙盒。

## 部署
- 已重新部署：
  https://67adb34e2d3c408f8df71bfe26154746.bj3.agentos-app.net

## 备份
- V1.9：`versions/app.js-V1.9文案升级-v24.js`（styles.css 自 v23 起未再改动）

---

# ClubOS V1.9.1 — 首页搜索框透明化

## 问题
首页 hero 区搜索框为白色不透明背景，遮挡主图人物/风景，视觉上显得厚重。

## 修复
- `.hero-search` 背景改为 `rgba(0,0,0,.32)` + `backdrop-filter: blur(12px)`，与顶部定位/头像 pill 风格统一；
- 增加半透明白色边框，减弱阴影，避免厚重感；
- 搜索图标、输入文字、placeholder 改为白色系，AI badge 保持橙色 accent。

## 校验
- styles.css 括号 798/798 平衡；
- 重新部署到同一 CloudStudio 沙盒。

## 部署
- 已重新部署：
  https://67adb34e2d3c408f8df71bfe26154746.bj3.agentos-app.net

## 备份
- V1.9.1：`versions/styles.css-首页搜索框透明化-v24.css`

---

# ClubOS V1.9.2 — 首页搜索框白玻璃样式

## 问题
用户希望搜索框进一步轻薄，参考松赞小程序做成清透白玻璃效果，不要暗色底。

## 修复
- `.hero-search` 背景改为 `rgba(255,255,255,.18)`，配合 `backdrop-filter: blur(14px)`；
- 边框亮度提到 `rgba(255,255,255,.55)`，阴影更轻；
- 图标/文字/placeholder 纯白并加投影，确保在各种 hero 图上可读；
- AI badge 改为白边透明底，融入玻璃质感。

## 校验
- styles.css 括号 798/798 平衡；
- 重新部署到同一 CloudStudio 沙盒。

## 部署
- 已重新部署：
  https://67adb34e2d3c408f8df71bfe26154746.bj3.agentos-app.net

## 备份
- V1.9.2：`versions/styles.css-首页搜索框透明白玻璃-v25.css`

---

# ClubOS V1.9.3 — 首页搜索框 80% 透明无边框

## 问题
用户希望搜索框再去掉白边框，整体做成约 80% 透明的底模块，更清透。

## 修复
- `.hero-search` 背景改为 `rgba(255,255,255,.2)`（80% 透明），移除边框；
- blur 降至 10px，阴影更轻；
- 图标/输入文字/placeholder 纯白并加强投影，确保可读；
- AI badge 去边框，仅保留浅白透明底。

## 校验
- styles.css 括号 798/798 平衡；
- 重新部署到同一 CloudStudio 沙盒。

## 部署
- 已重新部署：
  https://67adb34e2d3c408f8df71bfe26154746.bj3.agentos-app.net

## 备份
- V1.9.3：`versions/styles.css-首页搜索框80透明无边框-v26.css`

---

# ClubOS V1.10 — 类型/年龄/标题/AI方向/文案去重 综合修复

## 问题（用户五类反馈）
1. 活动主题生成太单调，没有吸引力；
2. 一句话写了「徒步」，活动类型还显示「亲子户外」；
3. 一句话写了年龄要求，系统自动生成且不好修改；
4. AI 建议方向太死，所有活动就 3 种方案，要结合时间/季节/地点/人文生成不同风格；
5. 详情页和分享文案话术单一、重复年龄/标题。

## 修复
### 活动类型下拉匹配
- 新增 `TYPE_OPTIONS`（value=内部标准类型，label=界面友好名称），`renderEditor` 的类型 select 改为带 value 属性动态渲染；
- 修复内部类型「徒步」与界面选项「徒步登山」不匹配，导致默认选中第一项「亲子户外」的 bug。

### 年龄尊重与保护
- `parseAgeRange` 解析到用户显式年龄时设置 `ageManual = true`，`recomputeDifficultyAndAge` 不再覆盖；
- 修复「20 岁以上」被误解析为「20 岁」的问题（调整「岁以上」匹配优先级高于「岁」）。

### 标题去泛化
- 重写 `titleSetFor`：从 `buildTitleCandidates` 中按当前方向 mood 挑选最贴合的标题，避免回退到「这个时节正好」「体验一次轻户外的魅力」等泛泛表达。

### AI 方向多元化
- 重写 `directionPoolFor`：
  - 通用徒步方向池从 8 个扩到 12+，按季节/周末/假期/地点气质拆分 headline；
  - 新增 `placeExtra` 地点专属方向（牛背山、青城山、四姑娘山、都江堰、川西、云南、城市绿肺等）；
  - `scoreDirections` 加大随机权重，增加地点/季节/周末加成，减少旧模板固定出现概率；
  - 重写 direction reason，把内部策略说明改成用户面向的短句。

### 详情页文案去重
- 批量移除 `COPY_DB` 各类型 intro 中的 `${a.title}` 和 `${a.ageRange}`，避免标题/年龄反复出现；
- `applyContentDirection` 中事实句过滤年龄，改为「关于这场活动：事实｜事实。方向理由」的温和语气。

### 地点语料扩展
- 「牛背山」加入 `PLACE_DB.川西.aliases`，使其获得川西的季节开场句与地点氛围句。

## 校验
- `node --check app.js` OK；
- Node 冒烟测试 20/20 通过。

## 部署
- 已重新部署：
  https://67adb34e2d3c408f8df71bfe26154746.bj3.agentos-app.net

## 备份
- V1.10：`versions/app.js-修复类型识别年龄与AI方向单一-v25.js`

---

# ClubOS V2.0 — 事实驱动重构（AI 杂志式活动页）

## 目标
从「AI 自由发挥」转为「事实驱动」：所有对外文案与页面区块必须基于已确认 / 已推断 / 缺失的事实状态，杜绝自动虚构；AI 只做洞察与编排，不编造事实。

## 关键改动
### 1. 事实层（Fact Layer）
- `FACT_SPECS`：20 个字段登记（距离、海拔、天数、费用、集合点、适合年龄、风险等级等）；
- `factRegistry(a)` 返回每个字段 `status`：`confirmed`（用户填）/ `inferred`（AI 推断，需确认）/ `missing`（缺失）/ `not_applicable`；
- `confirmFact(a, key)` 将 inferred 升级为 confirmed，未确认不进入客户页。

### 2. 洞察层 + 动态创意概念层
- `buildInsight(a)`：基于事实生成「为什么值得参加」洞察；
- `MOTIVATIONS`：8 大动机库；`buildConcepts(a)` 动态生成 3 个**动机互不重复**的创意概念；
- `buildContentDirections(a)`：内容方向池，按地点/季节/类型评分 + 随机权重。

### 3. 图片理解层
- `analyzeImageFocus` / `buildPhotoMeta`：返回 `category / orientation / quality_score / emotion / subjects / focal_point / safe_text_area / recommended_use / duplicate_group / simulated`；
- 分析结果带「模拟分析（演示）」标注，明确非真实 AI 视觉识别。

### 4. 动态区块编排
- `buildPageOutline(a)`：17 类区块（hero / story / gallery / insight / concept / schedule / safety / faq 等）；
- 图片分配器 `take(k)` 不重复用图；少图活动**不硬凑图组**（少图断言：无 image_pair / sequence）。

### 5. 发布前事实检查
- `runPublishCheck(a)`：返回 `blocking`（禁止发布）与 `warnings`（可提示后发布）；
- 例如全包行程未确认、`missing` 关键事实会进入 blocking。

### 6. 后台流程：事实确认 + 内容/视觉/发布 三 Tab
- `renderEditor` 重构为三 Tab：`内容`（事实确认区 + 原表单）/ `视觉`（图片分析 + 封面选择 + 页面结构）/ `发布`（发布检查 + 多平台文案 + 海报入口）；
- 新增 `factConfirmHtml` / `visualTabHtml` / `publishTabHtml` 三个渲染函数；
- `handleClick` 新增 `switchTab` / `confirmInfer` / `setCover` / `copyDraft` 四个 action。

### 7. 文案防模板 + Demo 降级标注
- 清除禁用模板句「重新认识自己」（原 3 处已在 V2.0 改写时移除，仅保留在 `TEMPLATE_PHRASES` 检测列表供发布检查）；
- 内容策略标题标注「CONTENT STRATEGY · 演示生成」，多平台文案标注「演示生成」。

### 8. 需求 11：弱化未接入入口
- 会员、分类筛选、主题详情、品牌故事、目的地、精选、城市切换等均以 `开发中` toast 占位；
- 报名结算标注「Demo 阶段仅展示，暂不接入真实支付」，不暴露会员/CRM/财务能力。

## 验收（5 案例）
| 案例 | 类型 | 人群 | 区块数 | 概念动机 | 发布检查 |
|---|---|---|---|---|---|
| 青城山亲子徒步 | 徒步 | 亲子 | 11 | 成长/低门槛/同行 | warn |
| 重庆两日游 | 城市旅行 | — | 9 | 3 不重复 | **blocking**（全包未确认）|
| 四姑娘山大峰 | 高海拔/登山 | — | 11 | 3 不重复 | warn |
| 虹口漂流 | 玩水 | — | 10 | 3 不重复 | warn |
| 再次亲子徒步 | 徒步 | 亲子 | 10 | 3 不重复 | warn |

- Node 验收脚本（`/tmp/v20_check.js`）**24 passed / 0 failed**：覆盖概念动机互不重复、动态编排、发布检查捕获冲突、关键事实断言（亲子受众、标题引用 6 公里/6—12 岁、城市旅行无虚构、高海拔难度评级无虚构承诺、少图不硬凑图组）。

## 校验
- `node --check app.js` 通过（4304 行）；
- 5 案例验收 24/24 通过。

## 部署
- 已重新部署到同一 CloudStudio 沙盒：
  https://67adb34e2d3c408f8df71bfe26154746.bj3.agentos-app.net

## 备份
- V2.0：`versions/app.js-V2.0动态编排三Tab-v27.js`
- V2.0 CSS：`versions/styles.css-V2.0三Tab部署-v27.css`
- 重构前基线：`versions/app.js-V2.0重构前基线-v26.js`
- 验收脚本：`/tmp/v20_check.js`

---

# ClubOS V2.0.1 — 事实门控与发布分层（v28 部署）

## 背景
v27 部署后，本地工作副本继续收敛了一轮改动（app.js 4304→4479 行、styles.css +165 行）。2026-08-31 检查「最新版本」时发现线上仍是 v27、本地已是 v28 候选；经 24/24 验收通过后，用户确认发布 v28。

## 关键改动（相对 v27）
- **事实门控（confirmed 前置）**：`distance / elevation / days / age / limit / services` 未确认为 `confirmed` 前，不进入客户页文案与详情页标签（`factsFact` 只展示已确认事实）。
- **发布检查分层**：关键推断（难度、服务，或高风险活动的年龄）未确认 → `blocking` 直接禁发；其余推断 → 仅 `warning`。高风险活动必须由老板确认适合人群（年龄）才放行。
- **内容方向改稳定打分**：`stableScore` 取代 `Math.random`，同一组输入产出一致方向组合（便于复用与测试）；与 V1.9/1.10「每次随机避免千篇一律」方向相反，属 v28 有意取舍。
- **解析增强**：集合点/返程时间正则更稳；新增从「行程：」文本自动 `parseItinerary` 拆多日行程。
- **FACT_SPECS 细化**：`difficulty` 仅「高海拔登山」必填、`age` 仅亲子类必填、`services` 取消必填。

## 校验
- `node --check app.js` 通过（4479 行）；
- 5 案例验收脚本 **24 passed / 0 failed**（重跑确认 v28 候选健康）。

## 部署
- v28 已重新部署到同一 CloudStudio 沙盒，线上与本地一致：
  https://67adb34e2d3c408f8df71bfe26154746.bj3.agentos-app.net

## 备份
- v28：`versions/app.js-V2.0事实门控发布分层-v28.js`、`versions/styles.css-V2.0事实门控发布分层-v28.css`
- v27：`versions/app.js-V2.0动态编排三Tab-v27.js`、`versions/styles.css-V2.0三Tab部署-v27.css`

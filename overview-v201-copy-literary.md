# v201 发版说明 · 文案不该是参数播报（文学层 / 事实层分层）

> 老板反馈：「为什么文案里面老是出现 时间 日期，我们要的文案是有语言美感的，不是这种没有艺术的数字。」
> 附两张截图：① 大标题「9月20日，把朋友约到青城后山（山脊山地）」、副标题「社交 · 9月20日 · 25 公里 · 中等强度」；
> ② AI 叙事段末尾整行「9月20日单日往返｜98元/人｜限15人｜4小时车程｜海拔800米」。

## 一、根因（两条独立成因）

| # | 现象 | 根因 |
|---|---|---|
| A | 标题 / 副标题 / 导语 / 正文 / 金句里都有日期与里程 | 本地角度模板 `EDITORIAL_ANGLE_VOICE` 把 `{D}`(日期)/`{startTime}`(时刻)/`{distWord}`(公里)/`{eleWord}`(海拔)/`{limit}`(人数) **直接填进文学层文案**，与 AI 端 prompt（早已禁止标题出现硬销信息）规则不一致 |
| B | 正文末尾整行参数播报 | AI 提示词要求正文「**最后给决策信息**」→ AI 就照着播报成「9月20日单日往返｜98元/人｜限15人｜4小时车程｜海拔800米」 |

## 二、设计：分层，而不是「删掉数字」

| 层 | 包含 | 数字处理 |
|---|---|---|
| **文学层** | 标题 / 副标题 / 导语 / 正文段落 / 金句 / 海报标语 / 宣发文案 / 消费叙事（为什么值得去 · 会体验什么 · 能得到什么） | 参数一律剥离或丢弃，只留画面与感受 |
| **事实层** | 数据条 / 决策速览 / DAY 时间轴 / 费用说明 / 报名结算 / 会员与营销面板 / 退改条款 | 精确数字**原样保留**（客户据此决策、下单、核对） |

`case-v201` 用「事实层逐字节不变」断言把这条边界钉死（行程时间轴 / 价格 / 名额 / 里程 / 海拔 / 费用含不含 / 年龄 / 退改条款）。

## 三、闸门（`core.js`）

```
factBroadcastHits(text)      → 列出硬参数（日期/时刻/价格/名额/里程/海拔/年龄/天数/时长车程）
sanitizeLiteraryText(text)   → 完整闸门：逐行 → 逐句 → 逐分句，剥离参数、丢弃参数播报、丢弃残句
stripBroadcastLines(text)    → 轻量闸门：只丢弃「参数播报行」，其余一字不动
sanitizeLiteraryList / stripBroadcastList
```

**两条硬约束**

1. **不产出破句**：参数**嵌在短语中间**时不硬删，而是整个分句丢弃。
   `把9月20日留给朋友。` → 整句丢弃（**不是**「把留给朋友」）；`走完这12公里，你会更清楚自己能走多远。` → `你会更清楚自己能走多远。`
   判定依据是 `isFactLiteralEdge`：参数两侧必须是段首/段尾/标点，才算「参数本身就是整个分句」。
2. **不连带丢好句**：**不做「参数播报行整句丢弃」的短路**。实测（契约 A2b）播报串常与正常句共处一句、中间没有句号：
   `9月20日单日往返｜98元/人｜限15人｜4小时车程｜海拔800米走路的时候，话题自然就有了。`
   整句丢弃会把后半句好文案一起带走 → 改为逐分句 + 逐参数片段处理。

**两档用法**（谁写的，决定用哪档）

- 系统生成（风格包 pack、AI 的 whyGo/experience/gain）→ **完整闸门**（作者是系统，改它没问题）
- 老板可能手写的（活动名称 / 导语 / 正文段落 / 金句 / 适合谁）→ **轻量闸门**（不替老板改他自己写的句子）
- 标题类 → `gateTitle`：走完整闸门，但**清成空就保留原文**（短标题清完常只剩空白，空白标题比一个日期更糟）

## 四、改动清单

- `src/core.js`：新增闸门（上表）+ 日期规则**不认**独立的「周X / 星期X」+ 中圆点残渣清理（`社交 · 9月20日 · 25 公里 · 中等强度` → `社交 · 中等强度`）；`checkRequiredModules` 登记 3 个新函数。
- `src/publish.js`：
  - `EDITORIAL_ANGLE_VOICE` 七个角度的 titles/leads/paras/quotes **全面去数字**（`{D}`→「这个周末/出发那天」，里程→「一路都在换视野」…）。
  - `fillFrames` 把事实型占位符（`D`/`dateShort`/`startTime`/`endTime`/`distWord`/`eleWord`/`limit`/`price`/`difficultyWord`）**一律填成空串** —— 文学层从源头拿不到参数，将来谁写了 `{D}` 也漏不出来。
  - `angleEditorialPack` 副标题改为不含日期（旧版是「社交 · 9月20日 · 25 公里 · 适中强度」）。
  - AI 提示词：删掉「最后给决策信息」，新增「正文类字段禁止出现任何具体数字参数」「用文学说法指代时间（这个周末 / 出发那天 / 一整天 / 早发晚归）」「｜分隔的参数串判为不合格」「数字只允许出现在专门的事实字段（itineraryDays / includedServices）」。
  - `applyAIResult` 落库时对文学层字段过完整闸门（`sanitizeLiteraryFields`）。
- `src/activities.js`：渲染层兜底（**老板看的是已存进 localStorage 的旧活动，落库闸门管不到历史数据**）——
  图文页 `heroTitle`/`sub`/`leadParas`/`sec.paras`/`quote` 两档闸门；简洁页**同款闸门在各自函数内定义**（两页是两个独立函数）+ 大标题过 `gateTitle`；`narrativeBlock`（whyGo/experience/gain）过完整闸门；`narrativeFitBlock`（适合谁）过轻量闸门。
- `case-v201-copy-literary.epilogue.js`：50 项断言，覆盖闸门本体（含 10 类参数识别、破句防护、干净句不误伤、换行保结构、标点清理）、七个角度全量模板无参数、`fillFrames` 屏蔽、AI prompt 四条约束、渲染层 before/after（脏风格包 + 手写标题 + 简洁页）、事实层逐字节不变。

## 五、验收

- `case-v201` **50/50 全绿**；全量 **37 个 epilogue 全绿**。
- 样例页 `v201-sample.html`（改前/改后真实渲染同框 + 闸门逐条实测表 + 事实层保留证明）；
  像素验证图 `v201-hero-verify.png`：改前「9月20日，把朋友约到青城后山（山脊山地）」/「社交 · 9月20日 · 25 公里 · 中等强度」
  → 改后「把朋友约到青城后山（山脊山地）」/「社交 · 中等强度」，而两侧时间胶囊「9月20日 · 天府广场」**都保留**（分层生效的直接证据）。
- bump 201 → 线上 `version.json = 201`，11 个文件本地/线上 MD5 全一致。

## 六、本轮新踩的坑

1. **跨函数误用**：上轮补丁在简洁页 `renderActivityPhone` 里引用了只在图文页 `renderActivityEditorial` 定义的 `gateOwn` → 整个简洁页 `ReferenceError`。**两个页面是两个独立函数，闸门必须各自定义**（契约 §D/§E 直接红）。
2. **「整句丢弃」的代价**：播报串常与好句同句且无句号，短路丢弃会连带好句 → **不要短路，一律逐分句处理**。
3. **日期规则误伤**：把「周X」当日期会误杀「走完回来，周一没那么难熬了。」（契约 B1 实测）。
4. **连续替换不重叠**：`/[·]\s*[·]/g` 只会把 3 个中圆点并成 2 个 → 必须用「成串」形式 `/[·](?:\s*[·])+/g`。
5. **断言不要写死格式**：时间胶囊渲染的是 `dateMD`（9月20日）而非 ISO（2026-09-20），窗口还装不下前置的图标 SVG → 断言应表达「事实层仍带着日期」这一语义。
6. **风格包 `angle` 校验**：`editorialStylePackOf` 会把「角度与该活动当前风格不一致」的包判为旧包而弃用（**正确的**产品逻辑）—— 样例页要还原「老板当时存下的包」，`angle` 必须取 `editorialVariantOf(a).angle` 的真实值。

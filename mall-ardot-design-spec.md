# ClubOS 商城 · ardot 正式设计稿规格（v82 对齐）

> 用途：ardot MCP 连接器启用并信任后，按本文档为商城全套界面在 ardot 画布上生成正式视觉稿。
> 功能现状：前端 app.js（v82）+ 后端五表/三服务已全部实现并部署。本文档仅做视觉走查与高保真还原，不改业务逻辑。
> 关键约束（Batch 2 收口结论）：**消费者视角（Storefront / 商品详情 C 端）严禁出现** 佣金、供货价、`riskLevel` 原始风控等级、B 端入口与任何内部经营措辞；内部信息只在俱乐部收益台与平台后台可见。

## 一、设计语言（沿用 ClubOS 松赞风，不得偏离）

| Token | 值 | 用途 |
|-------|-----|------|
| `--bg` | `#F7F4EE` | 全局米白底 |
| `--brand` | `#2F5D50` | 松绿主色（导航高亮 / 主按钮 / 标题点缀） |
| `--brand-700` | `#244A40` | 深松绿（按下态 / 二级结构） |
| `--brand-tint` | `#F2F6F4` | 极浅绿（卡片底 / hover） |
| `--accent` | `#C2873F` | 金棕强调（价格 / 关键数据 / CTA 描边） |
| `--line` | `#E7E1D6` | 细线分组（替代卡片描边） |
| `--serif` | `"Noto Serif SC", Georgia, serif` | 衬线标题字体 |

- 标题字体：Noto Serif SC（衬线）；正文：Noto Sans SC
- 原则：沉浸大图、细线分组、留白克制；**移除装饰性边框与卡片，以图片为绝对主视觉**
- 移动端优先（俱乐部运营与会员消费均在手机），画布按 **375×812** 基准出稿；后台/平台可出 1440 桌面稿
- 装备类型标签（对外唯一语义层）：`gearTypeLabel` → L1「普通装备」/ L2「专业装备」/ L3「技术装备」（对应 riskLevel，但消费者只见此中文语义，不见 L1/L2/L3 与风控字样）

## 二、界面清单（8 套画板，均需在 ardot 画布落地）

### 视角 A · 消费者门店（C 端 H5，renderStorefront）
- 顶部细栏 `ps-topbar`：返回 + 居中「装备商城」
- `store-hero` 品牌渐变头：`{俱乐部名}装备精选` + 副文案「跟着领队用过的装备清单挑，出发前一次备齐」+ `store-trust` 信任三条（✓ 正品直发 / ✓ 7 天无理由 / ✓ 售后统一受理）
- `领队推荐` 货架（mall-recs）：横向滑动卡片（封面 + 标题 + 价格），来自 `state.myRecs`
- `mall-cats` 分类胶囊（徒步/露营/研学/其他，去重自 products.category）
- `mall-grid` 商品网格：封面 `mall-ph` + 标题 + 副标题 + 标签（`gear-type` 装备类型 + 分类 + ★评分）+ 价格 `mall-price` + `mall-ship`「顺丰包邮」
- 底部 `frontTabbar("mall")`：首页 / 商城（高亮）/ 订单 / 我的
- **禁止出现**：佣金、供货价、riskLevel 原值、上架/移出按钮、B 端入口

### 视角 B · 俱乐部收益台（后台，renderClubMallConsole）
- `section-head`：标题「商城收益」+ 副文案「平台统一运营商品、定价、发货与售后；你只负责推荐，按成交拿佣金」
- `stat-grid` 4 张收益卡：在途佣金 ¥(pending+frozen) / 可结算 ¥available / 已到账 ¥settled / 在架推荐 N
- `我的推荐货架`：shelf-row（封面 + 标题 + ¥售价·单件佣金¥X（比例/固定）+ 上架/移出按钮），可「预览会员看到的商城」
- `商品库`：未上架商品同款 shelf-row，一键上架
- `佣金流转`：状态说明「待确认 → 冻结（售后期）→ 可结算 → 已到账；订单退款则自动冲销归零」+ 最近 5 条 comm-list（可进全部明细）
- 入口：后台 sidebar「装备商城」(view=mallConsole) 与「商城」(view=mall) 双入口，复用 renderMall 但 layout 包裹不同

### 视角 C · 平台商城运营（总平台，renderMallAdmin）
- `ps-topbar`：返回 + 居中「平台商城运营」
- `商品管理（平台）`：mall-admin-row（封面 + 标题 + 分类·riskLabel·供货¥·售¥ + 删除），「新增商品」
- 编辑表单 `apply-form`：商品名 / 售价 / 供货价 / 佣金模式(按比例%/固定¥) / 佣金值 / 风险等级(L1普通/L2一般户外/L3安全关键) / 保存
- `佣金结算（平台）`：comm-list 全量 + 「结算全部可结算」（available>0 时）
- 注：风险等级在此用 `riskLabel`（L1 普通 / L2 一般户外 / L3 安全关键），是平台内部字段，消费者端不暴露

### 视角 D · 商品详情（双视角，renderMallProduct）
- 大图画廊 `mall-detail-ph` + 标题/副标题 + 标签（gear-type + 分类 + ★）
- **C 端**：`mall-detail-price` ¥零售 + `mall-ship`「顺丰包邮 · 48h 内发出」；说明「正品直发，7 天无理由退换…」；主按钮「立即购买」(mallBuy)
- **俱乐部视角** (mallCtx=console)：`mall-detail-price` ¥零售 + 「单件佣金 ¥X」；说明「平台负责上架、定价、库存、物流与售后；俱乐部不接触货款…」；按钮「上架到我的推荐货架 / 从我的货架移出」(mallToggleRec)
- 严禁 C 端出现「单件佣金」「上架」等经营动作

### 视角 E · 活动×装备融合（活动详情内嵌，renderGearMall）
- 活动详情页「装备建议」区：自动匹配商城商品卡片（gear-mall-card）
- 匹配徽标 `gmc-match`「清单匹配」；标签 `gear-type g-{riskLevel}`（用 gearTypeLabel 中文语义）
- 排序优先级 匹配 > 安全 > 品质 > 评价 > 价格（**佣金绝不参与**）
- 归因 `sourceType=ai_gear_list`；点击进商品详情（C 端视角）

### 视角 F · 消费者会员页（C 端，renderMembershipH5）
- `mc-card`：会员名 / 已参加 N 场 / 装备消费 ¥X
- `mc-ben` 会员权益三条（老队员优先报名 / 装备推荐清单 / 活动记录留档，注明「以本俱乐部实际公告为准」）
- `mc-contact` 联系我们（微信 / 电话 / 地址）
- **禁止渲染** B 端「俱乐部中心」与任何招商/抽成/佣金话术

### 视角 G · 数据运营台（后台，renderAnalytics）
- 北极星指标：「每 100 报名 → 商城 GMV」
- ≥5 张指标卡 + 俱乐部排行 + 商品排行（均排除 `reversed` 订单）

### 视角 H · C 端首页（renderFrontHome，含商城入口语境）
- 分区：正在招募 / 推荐给你 / 即将出发（daysUntil 升序 0–30 天）/ 往期回顾 / 装备（gearPicks=我的推荐或评分最高 6 件，mall-recs）/ 关于品牌
- 底部 `frontTabbar("home")`：首页（高亮）/ 商城 / 订单 / 我的
- 已删除：目的地 tab、城市切换、AI 搜索标记、B 端招商条（promoBar）、ClubOS 俱乐部版 badge

## 三、交付物
- 一个 ardot 设计文件，含上述 **8 套画板**（A–H）
- 复用 ClubOS 松赞风版式（沉浸大图 + 衬线标题 + 细线分组），迁移为 ardot 原生节点，便于后续 D2C / 走查
- 设计 token 严格使用第一章表格值，不得自创色板

## 四、AI 出图提示词（用于 hero / 商品封面 / 品牌大图，ardot image-gen 模式）
- 通用风格锚点：`户外沉浸式摄影，自然光，浅景深，米白与松绿基调，极简留白，无文字水印，电影感`
- store-hero 背景：`高山草甸徒步场景，领队整理背包特写，晨雾，松绿与米白，电影级户外品牌视觉`
- 商品封面（按品类）：徒步→登山杖/徒步鞋/冲锋衣；露营→帐篷/睡袋/营灯；研学→双肩包/望远镜；统一 `产品静物于自然场景，柔光，留白背景`
- 关于品牌区：`俱乐部活动合照剪影，远山，暖调，叙事感`

## 五、启用 ardot 后执行顺序
Step1 创建画布文件（375×812 基准，套用设计 token）→ Step2 落地视角 A 门店首页 + D 商品详情(C端) → Step3 视角 E 活动×装备融合（重点打磨「清单匹配」徽标）→ Step4 视角 B 俱乐部收益台 + D 俱乐部视角 → Step5 视角 C 平台运营后台 → Step6 视角 F 会员页 + G 数据台 + H 首页 → 截图校验（重点核对 A/D/F 无内部经营信息泄漏）
